import { createTheme } from '@mui/material'

export const defaultTheme = createTheme({
	palette: {
		primary: {
            main: '#FE5621',
        },
		// second: '#2A435D',
		// third: '#FFF8EE',
		// link: '#4BFF3C',
	},
})
